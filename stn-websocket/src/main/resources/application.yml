spring:
  mvc:
    view:
      prefix:
      suffix: .html
  thymeleaf:
    cache: false
  devtools:
    restart:
      enabled: true
      exclude: static/**,templates/**
    livereload:
      enabled: true
    remote:
      debug:
        enabled: true
  redis:
    host: redis-server
    port: 6379
    database: 0
logging:
  level:
   root: debug
server:
  port: 8080